@model List<Notification>
<ul id="notifications-list">
    @{        
        for (int i = Model.Count - 1; i >= 0 && i > Model.Count - 6; i--)
        {
            <li class="notification-content">
                @Model[i].Content
            </li>
        }        
    }    
</ul>
